"""Data engineering team: ingestion, curation, filtering, validation, and
synthetic task generation. Publishes the `rl-tasks-dataset` artifact — the
contract that kicks off the training team's loop.
"""
